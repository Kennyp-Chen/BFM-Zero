# HT_BFM runtime dependencies for env_isaaclab
#
# Supported baseline: Linux x86_64, Python 3.11, CUDA 12.8.
# Isaac Sim is Linux-only and requires a CUDA-capable NVIDIA GPU.

# NVIDIA and PyTorch package indexes (required for Isaac Sim and CUDA PyTorch).
--extra-index-url https://pypi.nvidia.com
--extra-index-url https://download.pytorch.org/whl/cu128

# Isaac Sim 5.1.0.0. Accept NVIDIA's EULA/terms as required by the Isaac Sim
# installer before launching Isaac Sim.
isaacsim[all,extscache]==5.1.0.0; sys_platform == "linux"

# IsaacLab is installed from its source tree in the env_isaaclab setup. The
# original environment.yml uses:
#   -e ../IsaacLab2.3/IsaacLab/source/isaaclab[all,isaacsim]
# Clone the matching IsaacLab 2.3.x source and run its official installer
# (./isaaclab.sh -i) before installing this file. Do not replace this with the
# old isaaclab==2.0.2 dependency from pyproject.toml.

# CUDA-enabled PyTorch used by the current env_isaaclab environment.
setuptools<81
torch==2.7.0+cu128

# Core project dependencies.
easydict>=1.13
exca>=0.4.5
hf-xet==1.4.2
huggingface-hub==0.36.2
humenv @ git+https://github.com/facebookresearch/humenv.git@0548761a19b783c6a3548a14a8c4ea1ca35e94af
hydra-core==1.3.2
joblib==1.5.3
loguru>=0.7.3
lxml==6.0.2
mediapy>=1.2.3
ml-collections>=1.1.0
mujoco==3.8.1
notebook>=7.4.2
numpy==1.26.4
numpy-stl>=3.2.0
onnx==1.20.1
onnxruntime==1.26.0
open3d>=0.19.0
pot>=0.9.5
pydantic==2.11.10
scipy>=1.10
matplotlib>=3.7
rich==14.3.3
safetensors==0.7.0
# starlette is pinned by the installed IsaacLab release; do not override it here.
tensordict>=0.8.3
termcolor>=3.0.1
tyro>=0.9.18
wandb>=0.19
xxhash==3.6.0

# Optional but used by amp_stage2_play.py when --gamepad is enabled.
pygame>=2.6.0
